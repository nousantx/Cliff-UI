import Color from "./color";

const styleColor = () => {
  Color([
    "primary",
    "accent",
    "info",
    "success",
    "warning",
    "danger",
    "neutral",
  ]);
};

export default styleColor;
